apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc
  labels:
    app: redis
spec:
  accessModes:
    - ReadWriteOnce        # 单节点读写,Redis 单实例够用
  resources:
    requests:
      storage: 1Gi
  # 不指定 storageClassName,用 kind 默认的 local-path