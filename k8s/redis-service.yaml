apiVersion: v1
kind: Service
metadata:
  name: redis            # 这个名字 = FastAPI 用的 REDIS_HOST
  labels:
    app: redis
spec:
  type: ClusterIP        # 只在集群内部可达,Redis 不该暴露到外面
  selector:
    app: redis           # 把流量转发到带 app=redis 标签的 Pod
  ports:
    - port: 6379
      targetPort: 6379